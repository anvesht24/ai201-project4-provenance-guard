# ai201-project4-provenance-guard
A Flask backend that classifies submitted text as AI-generated or human-written using multi-signal detection (LLM + stylometrics), returns a calibrated confidence score and transparency label, and supports creator appeals with full audit logging.
